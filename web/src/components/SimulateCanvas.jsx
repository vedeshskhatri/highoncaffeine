/*
 * SimulateCanvas.jsx — Phase S1 placeholder
 * Phase S4 will populate with TempChart, DeltaAmbientChart, MetricCards,
 * HeatLossBreakdown, ValidationPanel.
 */
export default function SimulateCanvas({ result, request }) {
  return (
    <div className="canvas-placeholder">
      <div className="canvas-placeholder-title">Simulate</div>
      <div className="canvas-placeholder-hint">
        Charts and metrics will appear here after running a simulation — Phase S4
      </div>
    </div>
  );
}
